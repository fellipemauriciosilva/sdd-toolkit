# Expected — sdd-analyze-migration case-02

1. Inventaria o pouco que é observável e marca o restante como `unknown`
2. Não descompila binário automaticamente e declara essa limitação
3. Não propõe ondas detalhadas sem inventário mínimo
4. Lista a evidência ausente e a ação de validação para cada lacuna
5. Retorna `AGENT_RESULT` com `status: blocked` e `blocked_on` preenchido
