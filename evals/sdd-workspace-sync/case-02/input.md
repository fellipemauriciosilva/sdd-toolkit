# Eval Input — sdd-workspace-sync case-02
# Cenário: Nenhuma ativação → catálogo vazio, sem inferência

## Invocação
```
/sdd-workspace-sync
```

## Contexto
- `sdd activation list --json` retorna uma lista vazia
- Existem repositórios no disco do usuário, alguns com `.github/`
