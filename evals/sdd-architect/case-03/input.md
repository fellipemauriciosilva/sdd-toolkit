# Demanda

Trocar o mecanismo de autenticação da API pública e migrar tokens existentes sem indisponibilidade. O texto não informa provedor, formato dos tokens, volume, janela de migração ou política de rollback.
